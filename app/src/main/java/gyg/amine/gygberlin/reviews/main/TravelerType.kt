package gyg.amine.gygberlin.reviews.main

/**
 * enum class holding types of travelers, for simplicity the enumerated objects match the string of the traveler type
 */
enum class TravelerType {
    FAMILY_OLD, FAMILY_YOUNG, COUPLE, FRIENDS, SOLO
}